@model IEnumerable<Ticket>;
@{
    ViewData["Title"] = "All Tickets";
}

<h1>All Tickets</h1>
<br />
<br />


<br />
<br />
@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(T => T.EventID)</th>
                <th>@Html.DisplayNameFor(T => T.TicketCode)</th>
                <th>@Html.DisplayNameFor(T => T.TicketCategory)</th>
                <th>@Html.DisplayNameFor(T => T.TicketPrice)</th>

            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.EventID</td>
                    <td>@item.TicketCode</td>
                    <td>@item.TicketCategory</td>
                    <td>@item.TicketPrice</td>

                </tr>
            }
        </tbody>
    </table>
    
    <td>
        <Form asp-controller="Ticket" asp-action="DeleteEventTicket" m asp-route-id="@Model.FirstOrDefault().EventID" class="btn btn-info">
            <input type="submit" value="DeleteEventTicket" class="btn btn-danger" /a>
        </Form>
    </td>
}
else
{
    <div class="mt-4 alert alert-warning">
        <h4>there is no Tickets :(</h4>
    </div>
}
    

