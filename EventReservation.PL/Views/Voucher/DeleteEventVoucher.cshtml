@model IEnumerable<Voucher>
 
@{
    ViewData["Title"] = "All Vouchers";
}

<h1>All Vouchers</h1>
<br />
<br />


<br />
<br />
@if (Model.Count() > 0)
{
    <table class="table table-striped table-hover">
        <thead>
            <tr>
                <th>@Html.DisplayNameFor(V => V.EventID)</th>
                <th>@Html.DisplayNameFor(V => V.VoucherCode)</th>
                <th>@Html.DisplayNameFor(V => V.VoucherDiscount)</th>


            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.EventID</td>
                    <td>@item.VoucherCode</td>
                    <td>@item.VoucherDiscount</td>


                </tr>
            }
        </tbody>
    </table>


    <td>
        <Form asp-controller="Voucher" asp-action="DeleteEventVoucher" m asp-route-id="@Model.FirstOrDefault().EventID" class="btn btn-info">
            <input type="submit" value="DeleteEventVoucher" class="btn btn-danger" /a>
        </Form>
    </td>
}
else
{
    <div class="mt-4 alert alert-warning">
        <h4>there is no Vouchers :(</h4>
    </div>
}